using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Unsnaking : MonoBehaviour
{
	void OnTriggerEnter(Collider other)
	{
		if (other.CompareTag("EndSnaking"))
		{
			
		}
	}
}
